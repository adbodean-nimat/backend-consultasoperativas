import { SelectionRangeEnd } from '../../models';
/**
 * @hidden
 */
export interface CalendarComputed {
    [key: string]: any;
    activeRange?: SelectionRangeEnd;
    currentMin: Date;
    currentMax: Date;
}
